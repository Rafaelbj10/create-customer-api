spring.datasource.url=jdbc:mysql://localhost:3306/digibank
spring.datasource.username=root
spring.datasource.password=abcde123
# jpa
server.port=8080
spring.mvc.pathmatch.matching-strategy=ant_path_matcher

# Feign Configuration
feign.customer.config.default.connectTimeout=5000
feign.customer.config.default.readTimeout=5000
feign.customer.config.viacep-customer.connectTimeout=3000
feign.customer.config.viacep-customer.readTimeout=3000
# AWS Configuration
aws.region=us-east-1  # Substitua pela sua região
aws.sqs.customer-created-queue-url=http://localhost:4566/000000000000/account-service-queue
aws.access-key-id=your-access-key  # Ou use variáveis de ambiente
aws.secret-access-key=your-secret-key

# Logging
logging.level.customers.com.create.customer.ZipCodeClient=DEBUG
logging.level.feign=DEBUG
# Região AWS
spring.cloud.aws.region.static=us-east-1
# Credenciais fake (LocalStack)
spring.cloud.aws.credentials.access-key=test
spring.cloud.aws.credentials.secret-key=test
# Endpoint do LocalStack
spring.cloud.aws.endpoint=http://localhost:4566
# 🔥 MUITO IMPORTANTE (SQS)
cloud.aws.sqs.endpoint=http://localhost:4566
# Evita tentativa de buscar credenciais reais
spring.cloud.aws.credentials.instance-profile=false
# Timeout (opcional)
spring.cloud.aws.sqs.listener.max-number-of-messages=10
#Observabilidade
# Actuator - expor endpoints
management.endpoints.web.exposure.include=health,info,prometheus,metrics
management.metrics.tags.application=create-customer-api
# Tracing - 100% em dev, reduza em prod (ex: 0.1 = 10%)
management.tracing.sampling.probability=1.0
# Exporta traces via OTLP
otel.exporter.otlp.endpoint=http://localhost:4317
